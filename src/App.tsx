/**
 * Placeholder shell. The real three-pane layout lands in Task 15 — this exists
 * so the scaffold builds and the Playwright probes have an origin to run on.
 */
export function App() {
  return <main>GTDo</main>
}
